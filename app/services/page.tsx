import Section from "@/components/Section";

const ServicesPage = () => {
  return (
    <Section
      eyebrow="Services"
      title="Services Overview"
      description="Alternating story blocks, cards, and motion cues will be layered in future phases."
    >
      <p className="text-sm text-gray-600">
        Placeholder copy ensures routing works while we stage the visual language.
      </p>
    </Section>
  );
};

export default ServicesPage;
